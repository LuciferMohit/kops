# Gigifying Farming — Backend (Node.js + MySQL)

## Overview
This repo provides a minimal backend for the Gigifying Farming project:
- Auth endpoints (register, login, me)
- Users table in MySQL with flexible `role_meta`
- Demo user seeding script

The frontend `login.html` (already provided by you and left unchanged) can be used to call these APIs.

---

## Prerequisites
- Node.js 18+ (or 16+)
- MySQL 5.7+ / MariaDB
- Git (optional)

## Setup

1. Clone / copy files into a working folder.

2. Install dependencies:
```bash
npm install
